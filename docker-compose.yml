services:
  mysql:
    image: mysql:latest
    container_name: mysql
    volumes:
      - db:/var/lib/mysql
    networks:
      - matchaNetwork
    env_file:
      - ./.env
    ports:
      - "3306:3306"

  api:
    build:
      context: ./api/
    container_name: api
    volumes:
      - api:/api
    expose:
      - '3000'
    networks:
      - matchaNetwork
    depends_on:
      - mysql
    env_file:
      - ./.env

  websocket:
    build:
      context: ./websocket/
    container_name: websocket
    volumes:
      - websocket:/app
    expose:
      - '3001'
    networks:
      - matchaNetwork
    depends_on:
      - mysql

  frontend:
    build:
      context: ./frontend/
    container_name: frontend
    volumes:
      - frontend:/app
    expose:
      - '1212'
    ports:
      - 1212:1212
    networks:
      - matchaNetwork
    depends_on:
      - mysql

volumes:
  api:
    name: api
  websocket:
    name: websocket
  frontend:
    name: frontend
  db:
    name: db

networks:
  matchaNetwork:
    driver: bridge
    name: matchaNetwork